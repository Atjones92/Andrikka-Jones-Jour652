blockquote {
   .pullquote {
    font-size: 1.5em; /* Make the text larger */
    margin: 20px 0; /* Add space above and below the pull quote */
    padding: 10px 20px; /* Add padding inside the block */
    border-left: 5px solid #007bff; /* Add a left border */
    background-color: #f9f9f9; /* Light background color for contrast */
    color: #333; /* Text color */
    font-style: italic; /* Italicize the text */
}
}
a:link {
    color: blue;
}
a:hover {
    color: red;
}
